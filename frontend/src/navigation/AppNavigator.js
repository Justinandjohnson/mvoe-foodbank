// App Navigator - Root navigation with auth integration
import React from 'react';
import MainNavigator from './MainNavigator';

export default function AppNavigator() {
  return <MainNavigator />;
}
